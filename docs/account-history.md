# 账号操作历史功能卡

identity 拥有账号审计，web/shell 在团队管理下折叠展示。管理员需看到创建/重置、目标账号、操作者、凭据版本及时间；不返回密钥、校验值、请求键或请求指纹。

新增 GET /api/merchant/team/accounts/events，仅管理员、本商家可读，no-store。按不可变事件 ID 倒序，每页 50 条，before 正安全整数；下一页返回 nextBefore。无迁移或写入，无保留/备份策略变更。前端按需加载、显式刷新与翻页，错误和空状态可见；不自动反复扫描历史。

验收：创建和重置事件正确读取，超过一页无重复/遗漏，匿名及普通成员拒绝、跨商家隔离、无敏感字段；实际页面展开与手机宽度核对。下一批统一发布。

2026-09-11：完整 npm run check、生产依赖审计（0 漏洞）、部署脚本 9 项通过。107 条事件分页无重复，普通审核成员和匿名用户拒绝、不同管理员仅能读所属商家事件，响应字段白名单断言通过。浏览器读取上一轮实际页面开户与重置产生的两条记录，核对操作者、顺序及凭据版本 1/2；390px 宽度无横向溢出。未创建新服务器账号，临时服务已停止。本增量未发布，纳入下一批变更。
