# 现场处置慢网络状态功能卡

归属 web/review，复用 web/shared 的串行轮询。现有每 5 秒 setInterval 会在慢网络重叠读取，旧请求可能覆盖新结果。直播间切换已有 keyed remount，本次保留。

将处置状态读取改为串行、有 8 秒超时、取消与过期结果丢弃；提交处置前暂停后台轮询，完成后重新读取。无法确认时不再显示“暂无暂停记录”，复核解除在未获得新状态时不可执行；紧急暂停仍由服务端校验，不因读接口暂时失败完全封锁人工处置。

无接口、数据库、资产或权限变更。既有 pollResource 的并发、超时、迟到结果和取消测试覆盖机制；补充自定义超时文案验证，实际浏览器核对显示。完整检查后与下一批修复统一发布。

2026-09-11 验证：完整 npm run check 通过，生产依赖审计 0 漏洞，部署脚本 9 项通过。复用串行请求、8 秒截止、迟到结果丢弃与暂停取消测试；新增现场专用错误文案断言。独立 localhost:18967 浏览器登录后进入现场，停止测试服务，确认处置区显示网络错误及“尚未确认现场处置状态”，不继续展示旧暂停结论或可解除按钮。测试服务已停止，正式服务器未动。
