# 界面收敛补充核对

- 辅助工作区最大宽度收为 1360px，直播工作区仍保留 1800px，画面优先。
- 去除页面样式中偏青的深色背景，改为中性灰；状态仍保留文字和图标，风险提示保留红/琥珀色。
- Agent 折叠面板的标题和内容间距统一，减少重复大块留白。
- 在 900px 以下收起辅助双栏；手机表单单列，输入字号 16px、按钮至少 44px。
- 没有修改直播视频本身的颜色。

本轮构建和自动检查通过后才更新本地预览。当前电脑锁屏阻止浏览器工具访问，尚未完成本轮桌面、平板、手机的逐页视觉复核。源码检查不等于视觉验收，暂不部署服务器。

## 布局覆盖修正

900px 以下审核面板与内容导航显式归第一列，并取消跨行，避免单栏容器被旧的 `grid-column: 2` 撑出隐式列。Agent 专注状态使用更高优先级，避免按需加载样式的先后顺序影响单栏与字号。放大的直播提词器取消普通卡片的高度上限，内容过长时可滚动。

这三处修正已完成源码核对，真实浏览器复核仍受锁屏阻碍。

## 专注播讲操作

专注播讲使用原生 `dialog.showModal()`，由浏览器隔离背景操作和管理模态焦点；打开时聚焦退出按钮，Escape 或退出按钮关闭，正常返回时聚焦入口。展开期间锁定背景滚动，关闭或组件卸载时恢复原值。即使轮询发现讲稿解绑，退出按钮也仍可使用。

构建已通过。待解锁后的浏览器验收：Tab/Shift+Tab 不进入背景；Escape 返回入口；展开时解绑后可关闭；退出及切换页面后滚动恢复；390px 手机视口长稿可滚动。尚不将源码实现等同于这些交互全部验收。

## 解锁后实际页面核对

本轮浏览器已恢复访问。桌面截图确认直播画面占主区域、辅助提词器居右，界面采用灰白与中性深色。390×844 视口下检查主页、直播现场、商品与课程空状态；直播现场和商品页测得横向溢出 0。手机直播画面位置 x=12、y=132，尺寸约 366×363，辅助工具位于后方。临时视口已复原。

主页短卡片此前被同一行的长卡片拉高，现改为按内容高度显示，已在桌面和手机截图复核。上述不覆盖有资料时的全部子页，也不等于真机验收。

播放器新增连接/缓冲/暂停/结束/异常文字状态，连接或缓冲超过 15 秒提示异常；重连直接重建播放器，不要求再点第二次连接。构建已通过；当前本地房间已结束且无实际推流，因此本轮仅验证待机画面，重连与真实画面播放仍待后续场景验收。
