# 当前上线功能与模块化主分支整合

## 功能卡

- 目标：保留 5345a7c 已部署的灰白响应式界面、主页偏好、开播准入、经营者公示、投诉、现场处置、录像管理和成员权限等流程，同时采用 29e0d96 的模块和持久化归属；下次自动部署不得回退已上线行为。
- 主模块：live；协作：review、knowledge、content、agent、engagement、payments、analytics；身份和存储分别归 platform identity / infrastructure。
- 接口：保留当前 HTTP 兼容契约，跨模块使用 public 接口和装配注入；不恢复被删除的混合业务入口，不放宽架构门禁。
- 数据：保留现有数据库及 19 版迁移；新增功能对应表登记唯一所有者。录像字节与元数据、授权分别归基础设施与 live；不启用自动清理，不覆盖已保存文件。
- 权限及失败：保留租户隔离、独立审核、证据失效、撤销成员会话、资金幂等、Agent 故障隔离、录像登记失败提示；整合不得跳过已上线条件。
- 验收：完整 npm run check、依赖审计、部署保护测试；专项覆盖开播/推流重验、成员撤权、录像授权和存储恢复；界面实看主流程和移动布局。
- 发布：整合分支验证完成前不替换 5345a7c 正式服务；完成后按发布模板记录版本、备份及回滚，集中更新。

## 当前状态

2026-09-11：已确认主分支与部署分支分叉，开始在隔离工作树整合。尚未完成整合验证，不可合并发布。

### 界面迁移进展

已将上线分支的全部前端文件按模块迁入，直播控制、复盘统计和互动组件继续使用主分支抽取出的独立目录。新主页、团队、录像、投诉、活动及共享辅助组件已指定目录归属；没有恢复根目录业务界面。

独立前端类型检查通过（包含 vite/client 类型）；Vite 构建通过。后端仍有合并冲突，完整架构/业务/迁移检查尚未运行，不能据此前端检查发布。源工作树和当前正式服务保持不变。

### Agent 与身份整合进展

Agent 生命周期撤回查询与更新已迁入 agent/persistence，保留撤回后的队列失效、输出屏蔽和长任务中止。Agent 核心、服务、评测、长生成四组共 51 项测试通过；Agent 与身份公开入口独立类型检查通过。

身份模块已接入团队管理、数据库角色覆盖及会话版本校验；SQL 仅位于 identity/persistence。挂载团队路由时保持认证及角色中间件在先，停用账号登录返回 401。使用现有团队测试的完整断言在隔离身份路由上运行，两项测试通过；其中房间/商品读取为最小占位路由，仅验证身份与访问控制，不能替代整个平台集成测试。临时测试文件已移除，完整 team.test.ts 待后续装配完成后复验。

数据归属新增登记：agent_profile_revocations → agent；team_access、team_access_events → identity。均为上线分支已有表，不改变物理模式、职责或旧数据，不增加架构豁免。

尚未完成：原 app/content 混合入口的差异移植、跨模块装配、全量检查与浏览器复验。整合未提交、未部署。

### 内容授权及主页装配

生成来源由 content 查询，授权撤回和核验缓存由 review 查询，通过明确的 generationSources / authorizationIssue 接口组合，删除旧跨业务表联查。content_profile_revocations、content_authorization_checks 登记归 review；保留原表和数据。修复 Live 网关覆盖 Agent stale 标记的合并回退。内容、生成导入、Agent 网关及真实装配下的团队权限共 25 项回归通过；原混合 content.ts 已移除。装配入口按 strict 类型检查通过。

主页偏好归 identity，home_preferences 登记该归属；开播准备统计由装配层组合 knowledge / content / live 提供的数据，禁止身份模块直接查询其他模块表。主页持久化/版本冲突/跨租户及角色权限测试通过。

完整 membership 测试仍有两项失败：线下客户和互动权益入口尚未迁移挂载，返回 404。未将其排除或改成成功；接下来继续迁移。原 app.ts 的直播准入、录像、投诉、互动及复盘差异仍待处理。

### 客户与互动模块迁移

互动程序、签到、积分、礼品和兑换记录迁入 engagement，26 条 SQL 归该模块 persistence。观看资格通过注入 LivePort 读取，互动模块不查询 rooms/visits。5 项专项覆盖签到幂等、观看时长、库存竞态、退还和重启持久化，全部通过。

门店、来源码和线下业务记录归 customers（attribution_stores / attribution_sources / offline_batches / offline_records）；观看关联 attribution_visits 归 live。原跨模块 SQL 拆为授权来源校验与观看数据接口；首次有效来源登记保持在事务内，禁用来源不能新登记。analytics 组合 customers 的线下汇总与 live 的观看汇总，保留 conversionProven:false，不将来源关联宣称为成交因果。

客户、互动、成员权限合计 25 项回归通过，之前两项 404 失败消除；最终来源登记事务调整后 3 项 attribution 测试重跑通过。装配 strict 类型检查通过。这些模块未出现数据所有权检查错误；全量所有权检查仍因待迁移的 admission/app/投诉/录像等旧文件失败，未降低门禁。

### 公示、投诉与直播准入

公示资料、独立复核和投诉事件迁入 review，所属表登记为 disclosure_versions / disclosure_events / complaints / complaint_events；公开页面通过 live.room 接口定位经营者，不跨模块查询 rooms。

live_admissions / moderation_actions / moderation_results 归 live。现场处置通过 engagement.closeRoom 关闭活动，不查询其表；断流和结果持久化保持 Live 职责。准入消费 review 提供的公示状态、content 发布绑定和 live 暂停状态；开播及 MediaMTX/SRS 推流保持等待外部响应后的再次校验。已迁入开始记录、停止重置、独立解除及断流重试的完整路径。

准入、现场处置、公示、投诉 6 项集成测试通过；覆盖新开播拒绝、证据/日期失效、控制请求期间竞态、跨租户、自审拒绝、暂停后禁止重开和断流重试。装配 strict 类型检查通过。模块/平台代码未出现数据所有权错误；旧 app/录像等尚未完全移除，完整门禁仍未通过，不可发布。

### 录像登记、下载与巡检

recordings / recording_ingest_errors 登记为 live 所有。录像数据库操作移入 live/persistence；回执读取、隔离、文件验证、下载流和容量巡检移入 infrastructure，通过装配层创建并注入 Storage 能力。业务模块不自行拼接文件路径或调用文件系统 API。既有录像路径与回执格式保持不变，无文件移动、删除策略或数据迁移。

服务 tick 由 studio 暴露录像处理方法；相同数据库队列仍串行，登记成功与失败隔离保持原行为。登记/授权下载、路径和异常回执、存储健康、只读恢复副本巡检共 4 项测试通过，装配 strict 类型检查通过。

尚待迁移：注册录像备份和三个命令行/媒体钩子入口。旧工具导入路径暂未闭合；完整构建与架构检查仍待全部入口迁移后执行，不可发布。

### 整合检查完成（本地）

录像备份已拆为 live 的快照/登记遍历与 infrastructure 的受控文件副本操作；数据库驱动只在 persistence。备份、巡检和媒体钩子入口迁至 composition，示例和使用说明已更新路径。原 server/app.ts 移除，补齐漏迁的账本稳定分页及 /auth/me 角色覆盖查询。

最终本地 `npm run check` 通过：架构与数据所有权门禁、检查器负向测试、160 项业务测试、类型检查、构建及 HTTP 冒烟。`npm audit --omit=dev --audit-level=high` 无漏洞；9 项部署脚本测试通过。未增加架构豁免或放宽规则。

在独立 localhost:18964 演示环境实看主页和直播现场，灰白配色、画面优先布局保留；390×844 视口实测横向溢出为 0。未进行本轮真实设备/新推流复验。检查后已关闭独立测试服务和页面，原本地预览与正式服务不变。

此记录仅证明整合阶段的本地结果。需提交后的 GitHub CI 和必要的发布前核验，不代表真实模型、微信支付、实际资质审核或完整灾难恢复已完成。新入口路径需要随未来统一部署更新媒体服务配置，不能仅换应用后沿用旧录制钩子路径。

### 发布兼容补充设计

录制钩子和运维命令此前部署在 dist/server/server/recording-*.js，模块迁移后实际实现位于 composition。发布包保留三个旧命令路径的薄启动器，只导入新的 composition 入口，不保留旧业务实现、不新增架构豁免。构建后验证新目标存在，再生成固定映射启动器；这样既有 MediaMTX 配置无需在应用切换时同时改写，回滚也继续使用旧发布包自带命令。验证覆盖三个启动器能解析实际目标，以及缺少目标时构建失败。

兼容启动器验证：完整 npm run check 再次通过，161 项业务/构建工具测试通过；实际构建产物中旧巡检命令 --help 正常，旧钩子和备份命令在未配置参数时进入预期业务错误，而非模块找不到。Dockerfile 补入固定构建脚本和公开品牌资源；本机无 docker 命令，容器构建未实际执行，不声明容器验收通过。
