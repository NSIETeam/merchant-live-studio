# 发布验收记录：提审准备候选批次

## 发布前

- **目标**：测试服务器 `8.140.52.117` 的现有 `/studio/` 服务；候选分支 `feature/account-event-history`，发布前候选提交 `1e74c003c4a038887a033ff10aff673cf63c7d06`。包含账号创建/重置历史、角色生命周期、投诉申诉、平台信息、留存声明及平台级开播准入六个功能卡。合并提交 SHA 在 GitHub 合并后填写。
- **检查与产物**：候选提交本地 `npm run check` 通过 171 项业务测试、架构与数据归属检查、构建和 HTTP 冒烟；`npm audit --omit=dev` 为 0 个已知漏洞；部署脚本 9 项测试通过。最终 `/studio/` 前缀产物由 GitHub Actions 对合并后的确切 `main` SHA 重建，当前本地无前缀产物散列不作为服务器产物证明。
- **界面专项**：账号历史和权限生命周期、投诉与申诉、平台信息与留存声明、开播准入均已在真实浏览器操作。新增页面与抽屉在 390 × 844 视口无横向溢出，没有绿色状态色；详细证据见各功能卡。
- **迁移条件**：Live 数据库从已部署模式追加迁移 v20，新建 `complaint_appeals` 与 `complaint_appeal_events` 及不可修改触发器；不改写或删除既有表。账号事件使用既有平台表。平台与留存配置不入数据库。回退到旧应用时额外表可保留；自动部署失败会恢复发布前两个数据库快照。
- **配置前置**：代码可以在缺少 `PLATFORM_COMPLIANCE`、`DATA_RETENTION_POLICY` 时启动并公开标记未配置；生产开播将被新准入门禁拒绝。正式开播前必须在服务器私有环境文件配置并核验真实主体、政策链接和保存期限。自动部署不会写入或伪造这些资料。
- **备份与恢复**：自动部署在停止 Live/Agent 后使用 SQLite backup 分别保存两个数据库，执行完整性检查并记录散列；保留旧发布目录和环境配置。实际备份位置与校验结果待本次工作流完成后从服务器部署记录核对。
- **健康与关键流程**：部署脚本先确认无直播中房间，随后核对 Live/Agent 健康和首页。发布后另核对服务器报告 SHA、`/studio/`、`/studio/compliance`、公开合规接口、生产登录、旧房间读取和平台缺项开播阻塞。直播中则本次部署应被阻止，不能强制切换。
- **回滚路径**：旧基线为 `9e3a72f6e8540595e5cdd21f970e0f44514f2fcf`。新服务启动、数据库完整性、首页或关键接口失败时，由受控脚本恢复旧软链接和数据库快照并重新验证；回滚失败时保留维护状态供人工处置。

## 发布后

- **执行结果**：原候选及后续同日批次已统一进入主分支；当前已核对部署为 `ff9f2d6093921f4f6f98023158d78f7c86352d56`，[PR #10](https://github.com/NSIETeam/merchant-live-studio/pull/10) 与[主分支检查/部署](https://github.com/NSIETeam/merchant-live-studio/actions/runs/34634693418)均成功。
- **实际版本与产物**：工作流使用 `/studio/` 前缀重新构建目标 SHA，部署端回报同一完整 SHA；不是用本地旧产物替代。线上 `/studio/`、`/studio/api/health`、`/studio/api/channels` 均实际返回 HTTP 200。
- **实际备份**：部署端创建 `/opt/merchant-live-studio/backups/auto-ff9f2d6093921f4f6f98023158d78f7c86352d56-1789152175`；自动部署在切换前执行数据库快照和校验，仍需按运维周期做异机恢复演练。
- **健康与流程结果**：线上健康接口返回 API 总并发 256、观众并发 192；渠道接口返回微信身份、签名分享和真实支付均未配置。该可观察差异与目标版本一致。平台主体及留存仍未提供真实配置，因此生产准入不得视为通过。
- **真实能力验收**：真实模型、微信公众号、微信支付、真实运营主体、正式政策文本、ASR 与本次真实推流均未配置或未验证。演示支付不会转账；公开页面与准入保持这一实际状态。
- **交付状态**：该软件批次已部署并完成上述线上核对；业务资质、真实材料和外部渠道验收继续列为未完成，不能宣称已取得审核结果。
