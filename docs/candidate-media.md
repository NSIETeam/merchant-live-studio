# 隔离候选媒体接入

2026-09-11 服务器候选程序仍为 `569a08b`，本次只接入独立 MediaMTX 1.21.0 服务及录像配置，没有替换原直播服务。

候选入口：`https://8.140.52.117/studio-candidate/`。Live 18895、Agent 18896、HLS 18897、媒体控制 18898、RTMP 19360 均绑定回环地址。HTTPS 的 `/studio-candidate/media/` 代理至 HLS；媒体控制与鉴权回调仍不公开。

测试推流通过 SSH 隧道接入：

```sh
ssh -N -L 19360:127.0.0.1:19360 root@8.140.52.117
```

连接后 OBS 使用工作台提供的推流地址和该房间密钥。电脑本地 19360 必须可用，SSH 会话需保持。未开放公网 RTMP，手机推流及免隧道入口尚待正式部署安排。不要把 SSH 密码填进平台或推流密钥字段。

生产开播准入仍启用：没有完成经营者公示与独立定稿绑定的房间不能开播。没有导入虚构材料或直接修改数据库绕过校验。

## 留存配置

- 录像：`/var/lib/merchant-live-candidate-live/recordings`
- 完成事件队列：`/var/lib/merchant-live-candidate-live/recording-outbox`
- 媒体及 Live 进程使用 `live-studio`，目录仅服务账号可读写；每 10 秒完成的 fMP4 分段通过既有 hook 进入登记流程。
- 媒体服务限制 128MB、40% CPU，当前不自动删除录像。需继续完善生产容量告警、备份恢复和留存运营流程，不能长期无监控累积。
- 模板：`infra/mediamtx-candidate.yml.example`；真实共享密钥仅保存在服务器私有配置。

## 验证范围

六个原/候选服务均运行；候选登录和主要 API 正常。未知房间 HLS 返回 401，通过真实 RTMP 连接尝试的无效推流被拒绝。录像目录权限检查通过，巡检显示约 32.2GB 可用、无队列错误，当前 0 段录像。

这证明配置接通和未授权访问拒绝，**不代表候选已有成功直播或录像**。缺少真实主体/商品/独立审核材料，合法开播、录像登记下载与播放的候选端到端验收尚未完成。此前真实媒体播放、停播、录像及并发测试在隔离的本地测试环境完成，不能混写成服务器候选验收。

接入前配置备份：`/etc/merchant-live-candidate/before-media/`。回退需要先停候选媒体服务，恢复候选 live.env 与 Nginx 候选片段，检查配置后重启候选 Live、重载 Nginx；不改原服务配置，也不删除录制文件。
