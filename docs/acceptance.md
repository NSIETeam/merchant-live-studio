# 验收记录

## 完整交付开发增量：成员权限与独立审核

2026-09-11：94 项测试、类型检查、构建和 HTTP smoke 通过，当前未部署。新增用例覆盖同商家成员与独立账号身份、跨商家隔离、密钥和账本权限、开停播与发红包限制、配置非法映射、角色或凭据变化导致旧 Cookie 失效、自审拒绝、退回后的新版本重审、旧待审版本拒绝批准、决定不可覆盖及独立批准后主播绑定。

实际本地浏览器使用合成资料及独立测试账号完成：编辑保存 V3 → 提交 → 审核退回 → 编辑修改 V4 → 再次提交 → 审核批准 → 主播绑定。审核账号的正文与绑定按钮禁用；主播控制台展示 V4 和独立审核账号。原 V3 的退回说明仍保留。真实人员账号分发、生产迁移及线上角色验收尚未执行；逐条建议接受/退回和集中审批队列仍未完成。配置与升级要求见 [团队审核](team-review.md)。

## 完整交付开发增量：版本对照与绑定历史

本段是 0.5 之后的开发结果，不代表线上已经升级或完整目标已完成。2026-09-11，87 项自动化测试、类型检查、构建和构建后的 HTTP smoke 通过。新增检查覆盖两版证据快照、外商家读取拒绝、非法版本参数、删除与顺序变化、旧版本保留、重复绑定幂等、历史不可修改/删除、历史写入失败时绑定回滚，以及 v4 数据迁移后不补造操作者或重复记录。

本地实际浏览器使用既有合成课程验证：V1 的无依据比较正文与 V2 删除后的正文并列，新增过渡单独展示，各自证据保留；直播台显示迁移的历史关系，并明确未知操作者与记录不能证明实际开播。独立审核角色、逐条建议接受/退回及其他完整需求继续见 [完整交付跟踪](delivery-plan.md)。

验证日期：2026-09-11。开发机 macOS arm64 / Node.js 24.20.0；服务器 Debian 13 x86_64 / 独立 Node.js 24.20.0 / MediaMTX 1.21.0。

## 0.4.0 资料导入与场景评测

2026-09-11 新增底座验收：73 项测试、类型检查、构建与真实 HTTP smoke 通过；包括新增 5 项材料、9 项 Agent 评测服务、3 项训练网关检查。覆盖待审导入、来源与授权声明、草稿版本、幂等与冲突、整批队列回滚、任务失败/重启、跨租户、报告快照和资料变更后的评分拒绝。评测排除动态红包倒计时，相同输入重试不会随倒计时误报冲突。

本地实际浏览器完成模拟 CSV 预览与待审导入、原创样例新草稿、六场景双版本共 12 项比较以及人工意见保存。390×844 视口无横向溢出。两版输出均标明本地规则，不把 12 项检查通过称为品牌训练或完整讲稿批准。

测试服务器更新后核对 61 个部署文件，原直播数据及提示词/任务数据在迁移前后保留。真实 HTTPS 接口完成待审资料导入及重放、新 V3 草稿（原已发布 V2 不变）、双版本评测、人工意见保存与 Agent 重启后的持久化检查；评测结果不进入直播建议卡。媒体进程未重启，原站首页/healthz/readyz 与直播入口均为 200，模型保持关闭，合成测试房间均已结束。

小规模并发回归仍为本机 20 个并发观众、400 次读取/心跳，业务请求 0 失败，本次 p95 10.10 ms；模拟红包数量/金额守恒，Agent 停止后原直播、领取与 worker 仍可运行。它不是公网容量结论。

新增数据导入不包括自动解析 Word/PDF/录屏，不包括可复用商品/SKU 库、长稿写作、课程计划或人工讲稿审批。操作与边界见[资料与评测](training.md)。本轮 CI 仍应以最终提交的实际运行记录为准。

## 0.3.0 自动化验证

- `npm run check`：56 项测试通过，包括 16 项 Agent 核心、11 项 Agent 服务、3 项网关和原有 26 项直播回归；TypeScript 检查、Web/直播 API/Agent 构建及构建后真实 HTTP 闭环全部通过。
- 覆盖跨租户、公开秘密隔离、Origin/JSON/伪造 Cookie/大请求拒绝、注销重放与凭据撤销、代理信任与登录限流、本场资格重置、毫秒计时、暂停/隐藏/断线、并发红包金额守恒、重复领取、outbox 重跑、故障回滚、到期退款、事实依据、发布鉴权及断流失败处理。
- 构建后 HTTP 测试覆盖首页、观众路由、JS 资源、商家登录、房间、活动、心跳、领取、自动 worker、统计和源码不可公开读取。
- `npm audit --omit=dev --audit-level=high`：本次 0 个已知漏洞。首屏 JS gzip 约 91 KB；HLS 独立包按需加载，gzip 约 185 KB。HLS 体积提示不影响构建成功。

`forced-outbox-failure` 是主动注入的事务回滚用例。测试不调用真实支付。

## 真实媒体和服务器

| 检查               | 实际结果                                                                                                                       |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------ |
| 本机真实媒体链路   | MediaMTX + FFmpeg 的 10 项检查通过，包含 H.264/AAC 解码                                                                        |
| 服务器生产模式     | HTTPS 证书正常校验；禁止演示商家登录，实际访问密钥可登录                                                                       |
| 服务端严格资格     | 无信号、暂停、结束状态均不继续累计；领取需要新鲜有效心跳                                                                       |
| 服务器媒体/业务    | 8 组通过：错误密钥拒绝、正确源上线、HTTPS HLS 跳转/分片/音视频解码、资格、模拟领取幂等、账本、worker、分析、结束断流与重连拒绝 |
| 公网端到端         | 另一台独立服务器直接向公网 RTMP 发布，HTTPS HLS 下载 86,856 字节分片并解码 H.264/AAC，结束后确认源断开，3 组通过               |
| 既有直播数据持久化 | 直播及 Agent 重启后，业务数据、提示词版本、试演与反馈保持一致；本次 Agent 更新期间媒体进程未重启                               |
| 既有服务           | 原站首页、healthz、readyz 在部署和测试后均为 HTTP 200                                                                          |

初始媒体测试的上行使用 SSH 隧道，公网验收另用独立主机直接发布，二者没有混称。本机直接公网 RTMP 曾握手失败，独立主机也超时；检查确认主机防火墙缺少 1935 入站规则。备份并仅添加该端口规则后，独立公网验收通过。

测试均使用清楚标注的合成画面和测试音。展示编码器最长运行 600 秒，验收后已停止，所有测试房间已结束；服务本身保留供继续测试。没有让合成画面伪装成真实商家直播。

## 浏览器验收

在实际服务器 HTTPS 站点使用桌面 Chromium 浏览器验证：

- 商家认证和带 `/studio/` 前缀的资源、观众链接正确。
- 真实 640×360 视频持续播放，`currentTime` 从 43 秒推进至 157 秒，`readyState=4`；有效观看累计到 2:29。暂停后画面时间和资格均保持不变。
- 观众提问成功，商家工作台收到同一问题。
- 添加待审核事实后不会立即用于提词；审核后建议显示该事实及依据。风险话术出现对应提示，提示不作为法律判断。
- 390×844 视口检查并修复横向溢出，最终页面宽度为 390 像素。它是桌面浏览器窄屏验证，不等同于真机移动端验证。
- 发现并修复 Chromium 原生 HLS 误报支持导致的播放中断，现优先采用 hls.js/MSE，保留原生回退。

## 独立 Agent 新版

- 独立进程、独立 SQLite、私有服务认证、租户隔离；Live Core 仅依赖 HTTP 契约，不读取提示词实现或模型密钥。
- 本地 16 项核心、11 项服务、3 项网关联调测试全部通过。模型 HTTP 测试使用模拟响应，没有真实模型调用。
- 自动化覆盖不可变版本、草稿/发布、任务输入快照、幂等与冲突、全局/租户任务上限、并行数、重启和关闭、反馈、证据撤回后的过期标记、服务错误脱敏。
- `/agent/latest` 只查询 `live` 任务；这类任务入队时必须采用已发布版本。较新的 `rehearsal` 草稿不会覆盖直播建议卡。任务读取、最新建议、创建请求的幂等重放响应和反馈响应都会重新核对当前场次、时间及引用事实撤回状态，不把旧建议重新变为有效。
- 服务器已新增独立 Agent 账号、数据库和服务，模型状态明确为未配置；原直播房间 6 条、事实 1 条、领取 1 条、账本 3 条在升级前后保持一致，原站首页/healthz/readyz 与新版入口均 HTTP 200。
- 已在公开测试站实际浏览器创建品牌风格，保存并查看不可变 V1/V2 草稿，发布 V2。输入“无出其右，会让所有人想起妈妈的味道”出现三项风险提示，模型未配置/本地规则状态显示清楚。

公开站点已实际完成 V2 试演并保存反馈；结果显示本地规则、四个执行阶段和事实出处。390×844 视口页面宽度为 390 像素，无横向溢出。最终服务更新及两次 Agent 重启后，2 个风格、3 个不可变版本、1 次试演、1 条反馈和品牌 V2 发布状态全部保留。

服务器额外验证：主动停止 Agent 后，原站首页/healthz/readyz、直播页面、直播健康接口及公开房间接口仍为 HTTP 200，Live 进程继续运行，媒体进程未被重启。恢复 Agent 后状态正常、模型仍未配置。两个服务账号均不能读取对方数据库或环境配置。此次更新后的即时可用内存约 900 MiB；它不是容量承诺。

## 小规模并发与故障隔离

运行 `npm run build && npm run concurrency:smoke` 可复跑独立 Live/Agent 进程的 HTTP 测试。本次默认 20 个并发匿名观众、400 次公开房间读取/观看心跳请求，本机测量 0 失败，交互请求 p95 为 10.29 ms。启动就绪重试单独统计，不算业务失败或延迟。这是当次 macOS 本地工作负载结果，不是公网或生产容量结论。

15 个随机红包共 10007 分，数量与金额守恒，重复请求保持幂等。主动停止测试 Agent 进程后，公开直播间读取、新心跳、已有红包领取与模拟 worker 仍继续工作。临时进程与数据库已清理。

该测试显式使用 `REQUIRE_PLAYBACK=false`，无真实视频、真实支付或线上压测；不能据此推导万人容量。真实媒体证据来自前述服务器链路测试。

## GitHub 与协作

仓库已公开，三位原仓库协作者按原权限发出邀请，交付时仍待接受，见[协作者记录](collaboration.md)。每次推送执行测试、构建、HTTP smoke 和生产依赖检查，当前结果见 [GitHub Actions](https://github.com/NSIETeam/merchant-live-studio/actions)。旧私有仓库首轮运行曾被账户 Billing 阻止；不能将该次未执行的任务视为测试失败或通过。

上述 0.3.0 自动化与并发结果来自本地执行。本轮 GitHub CI 结果须在实际推送后独立核对，不从本地通过或旧版工作流状态推定。

## 尚未接入或未验证

- 微信 OAuth、商户准入、用户收款授权、真实出款、回调验签和对账；红包仍为模拟。
- 远程 LLM、实时 ASR、完整语义与行业合规引擎；已新增可切换模型适配接口，用户明确选择稍后配置，当前运行仍是基于人工审核事实的本地规则。提示词试演与反馈不等于模型微调。
- OBS 应用/硬件、手机 Safari/Android 真机、微信内播放器、SRS 实际媒体链路、Docker 容器、压力与恶意流量测试。
- 当前为单实例测试部署，没有 CDN、分布式抢红包、可用性承诺或商业上线验收。

下一阶段优先做 OBS 与微信/手机实际播放，再接正式身份、防刷、LLM/ASR provider 和经授权的微信支付产品。并发目标确定后再验证 PostgreSQL、Redis、队列与 CDN 是否必要。

## 0.5：靠谱内容工作台与观众页

2026-09-11，本地已完成 84 项自动化测试、类型检查、前端/双服务构建与真实 HTTP smoke。新增内容域测试覆盖商家隔离、商品及讲稿版本冲突、事务回滚、计划/课时修改、风险阻断、人工定稿、绑定、商品及规则版本失效、重开数据库保存；HTTP Agent 桥接额外验证版本化事实、类别、空事实评测失效和评分拒绝。

实际浏览器已走通商品 → 45 天计划 → 单课 → 含比较性承诺的 V1 阻断 → 修订 V2 → 商家人工确认 → 绑定 → 逐段播讲，并检查字号及专注阅读。材料明确为软件验收的合成设定，不能用来证明真实商家的风格、课程质量或商品宣称。

观众页默认三个面板全部收起。桌面 1280×720 的视频舞台高 596 像素（82.8%），手机 390×844 的舞台高 711.75 像素（84.3%），页面无横向溢出。此处指视频容器；实际内容按源视频比例完整显示，不裁切或拉伸。真实视频、真机与微信内核的验证范围需分别记录，不能由布局尺寸推定。

本轮本地小规模并发测试为 20 个观众、400 次业务读写，0 失败，当次 p95 24.06 ms；15 个模拟红包共 10007 分，数量金额守恒；停止 Agent 后直播业务继续运行。不是公网压力测试、万人容量或真实资金反作弊验收。

部署时采用独立 0.5.0 发布目录、双数据库备份、只更新本项目代理片段，并保留旧版回滚路径。最终部署版本通过服务器 `current/REVISION` 与 `artifact-manifest.json` 核对，CI 通过状态以对应 Git 提交的 Actions 运行结果为准；本地测试不替代这两项检查。

### 0.5 公网补充验收

本次运行代码为提交 `9b37c86b4be51d92e5b3b9955931ffbe3f23b7fe`，对应 [CI 运行成功](https://github.com/NSIETeam/merchant-live-studio/actions/runs/34560285896)。此后的文档补充不改变该部署的运行代码。

- 升级前后核对既有直播与 Agent 表记录数，原数据保留；75 个发布文件经 SHA-256 核对，11 个公开页面资源的实际 HTTPS 内容也与构建文件一致。原站首页、健康检查及本项目页面保持 HTTP 200，媒体进程在升级期间未重启。
- 真实 HTTPS 路径完成商品、45 天计划、课次、两版讲稿、人工定稿和绑定。10800 字的合成长稿请求为 33158 字节，穿过代理成功保存；它是重复测试文字，不是已生产的课程内容。
- 新增计划和课次编辑经过实际浏览器保存；定稿正文、依据、确认记录继续保留。直播台可读取 V2、切换段落、调整字号和进入专注播讲；观众只能读取绑定商品名称，不能读取私有稿件和证据快照。
- 主动停止 Agent 后，内容读取和 Live 健康接口仍可用；服务恢复正常。Live 重启后讲稿绑定及确认仍存在。这不代表内容域已独立部署：当前内容与 Live 业务同进程同库，绑定读取异常时主播页会隐藏讲稿。
- 本机直接向服务器公网 RTMP 推送清楚标注的 360×640 合成竖屏 H.264/AAC 测试源。HTTPS 浏览器实际解码播放，`readyState=4`，画面时间从 7.83 秒推进到 126.25 秒；点击原生暂停按钮后，画面时间在两次间隔读取中均为 126.248129 秒，有效观看均为 2:03 且显示暂未累计。恢复后继续播放。
- 观众页默认收起提问、红包和观看信息。390×844 的真实播放页面无横向溢出，视频舞台高 711.75 像素；达到观看条件后领取 ¥2.00 演示红包，页面明确不发生转账。测试提问在商家端显示同一正文。
- 本次测试源于北京时间 12:02–12:08 运行，随后主动停止编码器、结束测试直播间；确认媒体连接断开、观看端显示直播已结束。模型仍未配置，测试没有调用真实支付。

上述窄屏检查使用桌面浏览器视口，仍不等同于微信或移动真机验收。后台隐藏场景此次未新增人工浏览器结论；已有自动化覆盖不能代替具体移动系统的后台行为验证。
